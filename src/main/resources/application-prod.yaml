spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    org.hibernate.SQL: WARN
    root: INFO

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false
